#####################
# Unit tests
#####################

facts("StudentsTDistribution unit tests") do
    context("StudentsTDistribution() should initiatize a gamma distribution") do
        @fact true => false 
    end

    context("uninformative() should initialize an uninformative gamma distribution") do
        @fact true => false 
    end
end
