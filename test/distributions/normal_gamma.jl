#####################
# Unit tests
#####################

facts("NormalGammaDistribution unit tests") do
    context("NormalGammaDistribution() should initiatize a gamma distribution") do
        @fact true => false 
    end

    context("uninformative() should initialize an uninformative gamma distribution") do
        @fact true => false 
    end
end
