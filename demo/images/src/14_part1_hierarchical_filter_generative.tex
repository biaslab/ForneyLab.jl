\documentclass{minimal}

\usepackage{tikz}
\usetikzlibrary{calc, arrows, fit, positioning}
\definecolor{clamped}{RGB}{200,200,200}

\begin{document}
\begin{tikzpicture}
    [
        node distance=15mm,auto,>=latex',
        box/.style={draw, minimum size=.5cm},
        bbox/.style={draw, minimum size=1cm},
        blackbox/.style={draw, fill=black, minimum size=0.25cm}
    ]
    \tikzstyle{line} = [draw, -latex,>=latex]
    \tikzstyle{dash} = [dashed, -latex,>=latex]
    \tikzstyle{branch} = [fill,shape=circle,minimum size=3pt,inner sep=0pt]
    
    \begin{scope}
        % Bottom layer
        \node (x_l1_0_dots) {...};
        \node[left of=x_l1_0_dots, node distance=0.5cm] {$x^{(1)}_{k-1}$};
        \node[box, right of=x_l1_0_dots, node distance=1.0cm] (l1_add) {+};
        \node[right of=l1_add, node distance=1.0cm] (x_l1_N_dots) {...};
        \node[right of=x_l1_N_dots, node distance=0.5cm] {$x^{(1)}_{k}$};
        \path[line] (x_l1_0_dots) edge[-] (l1_add);
        \path[line] (l1_add) edge[-] (x_l1_N_dots);

        % Vertical path from bottom layer upwards
        \node[box, above of=l1_add] (l1_N) {$\mathcal{N}$};
        \node[box, above of=l1_N] (l1_log) {log};
        \node[box, above of=l1_log] (l2_eq) {=};
        \path[line] (l1_add) edge[-] (l1_N);
        \path[line] (l1_N) edge[-] (l1_log);
        \path[line] (l1_log) edge[-] (l2_eq);

        % Second layer
        \node[right of=l2_eq, node distance=1.0cm] (x_l2_N_dots) {...};
        \node[right of=x_l2_N_dots, node distance=0.5cm] {$\gamma^{(2)}_{k}$};
        \node[box, left of=l2_eq] (l2_add) {+};
        \node[left of=l2_add, node distance=1.0cm] (x_l2_0_dots) {...};
        \node[left of=x_l2_0_dots, node distance=0.5cm] {$\gamma^{(2)}_{k-1}$};
        \path[line] (x_l2_0_dots) edge[-] (l2_add);
        \path[line] (l2_add) edge[-] (l2_eq);
        \path[line] (l2_eq) edge[-] (x_l2_N_dots);

        % Vertical path from second layer upwards
        \node[box, above of=l2_add] (l2_N) {$\mathcal{N}$};
        \node[box, above of=l2_N] (l2_log) {log};
        \node[box, above of=l2_log] (l3_eq) {=};
        \path[line] (l2_add) edge[-] (l2_N);
        \path[line] (l2_N) edge[-] (l2_log);
        \path[line] (l2_log) edge[-] (l3_eq);

        % Top layer
        \node[right of=l3_eq, node distance=1.0cm] (theta_N_dots) {...};
        \node[right of=theta_N_dots, node distance=0.5cm] {$\theta_{\gamma, k}$};
        \node[left of=l3_eq, node distance=1.0cm] (theta_0_dots) {...};
        \node[left of=theta_0_dots, node distance=0.5cm] {$\theta_{\gamma, k-1}$};
        \path[line] (theta_0_dots) edge[-] (l3_eq);
        \path[line] (l3_eq) edge[-] (theta_N_dots);
    \end{scope}
\end{tikzpicture}
\end{document}